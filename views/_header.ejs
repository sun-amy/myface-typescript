<header class="header-container">

    <button class="nav-toggle" id="nav-toggle" type="button">
        <i class="fa-solid fa-bars"></i>
    </button>
    
    <div class="nav-menu" id="nav-menu">
        <a href="/users" class="nav-link">Users</a>
        <a href="/posts" class="nav-link">Posts</a>
        <a href="/posts/create" class="nav-link">New Post</a>
        <img class="logo" src="https://i.ibb.co/DwWTLkY/logo.png" alt="logo">
    </div>
    <script>
        const navMenu = document.getElementById("nav-menu");
        const menuButton = document.getElementById("nav-toggle");
        menuButton.addEventListener("click", function () {
            navMenu.classList.toggle('menu-open');
        });
        
        document.addEventListener("click", function (event) {
            if (navMenu.classList.contains("menu-open") && !navMenu.contains(event.target) && !menuButton.contains(event.target)) {
                navMenu.classList.toggle('menu-open');
            }
        });

        document.addEventListener("keydown", function(event) {
        if (event.key === "Escape" && navMenu.classList.contains("menu-open")) {
            navMenu.classList.toggle('menu-open');
        }
        });
    </script>
</header>


